import requests
import os
from dotenv import load_dotenv

def get_token():

def run():
    load_dotenv()
    client_id = os.getenv("CLIENT_ID")
    client_secret = os.getenv("CLIENT_SECRET")
    get_token()

if __name__ == "main":
    run()